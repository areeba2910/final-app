extends ../layout


block content
    .row
        .col-md-6
            h1 check new albums 
        .col-md-6
            a(href="/products/add" class="btn btn-primary float-right") Add song
        table.table
            thead
                tr
                    th song
                    th released in
                    th singer
                    th rate it
                    th comments
            tbody
                each product in products
                    tr 
                        td=product.name
                        td=product.price
                        td=product.city
                        td=product.location
                        td=product.detail
                        td  
                            a(href="/products/delete/"+product._id    class="btn btn-danger") delete
                            a(href="/products/edit/"+product._id    class="btn btn-info") edit
                            a(href="/products/cart/"+product._id    class="btn btn-info") add